import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

interface PartySaveScreenProps {}

const PartySaveScreen = ({}: PartySaveScreenProps) => {
	return (
		<View>
			<Text>Party Save Screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default PartySaveScreen;
