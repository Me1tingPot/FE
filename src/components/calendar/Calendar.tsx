import { StyleSheet, View } from 'react-native';

// 각 화면에서 사용하는 페이지를 기준으로 폴더를 나누어 컴포넌트 분리

interface CalendarProps {}

const Calendar = ({}: CalendarProps) => {
	return <View></View>;
};

const styles = StyleSheet.create({});

export default Calendar;
